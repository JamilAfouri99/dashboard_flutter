enum ConfirmationDialogAction {
  delete,
  add,
  update,
  cancel,
}

enum Token {
  access,
  refresh,
}

enum Links {
  facebook,
  whatsapp,
  gmail,
  linkedin,
  twitter,
  instagram,
  github,
  outlook,
  skype,
  slack,
  qcarder,
  link,
}

enum Flags {
  jordan,
  uae,
}
